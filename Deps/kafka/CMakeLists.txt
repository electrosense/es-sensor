FIND_PATH( KAFKA_INC NAMES rdkafka.h PATHS ENV C++LIB ENV PATH PATH_SUFFIXES librdkafka)
IF( KAFKA_INC )
        message(STATUS "Kafka found at ${KAFKA_INC}")
        FIND_LIBRARY( KAFKA_LIBRARIES NAMES rdkafka PATHS ENV C++LIB ENV PATH PATH_SUFFIXES lib lib64 )
#        FIND_PATH( OPENNI2_LIB_PATH NAMES libOpenNI2.so PATHS ENV C++LIB ENV PATH PATH_SUFFIXES lib/openni2 lib64/openni2 lib lib64)
 	if (KAFKA_LIB)
            include_directories(${KAFKA_INC})
#            SET(DEPS "${DEPS} libopenni2-0, libopenni2-dev,")
    endif()
ELSE()
        message("KAFKA library not found")
ENDIF()
